<%- model_class = Precencium -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:hora) %>:</strong></dt>
  <dd><%= @precencium.hora %></dd>
  <dt><strong><%= model_class.human_attribute_name(:observacion) %>:</strong></dt>
  <dd><%= @precencium.observacion %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              precencia_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_precencium_path(@precencium), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              precencium_path(@precencium),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
